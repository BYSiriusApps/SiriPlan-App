# SiriPlan Sistem Revizyonu — 18 Temmuz 2026

## ⚡ SİZDEN İSTENEN İKİ ŞEY

### 1. Vercel deploy (kod GitHub'da hazır, otomatik deploy tetiklenmedi)

Push (`7b26bd7`) GitHub'a gitti ama Vercel 20 dakika içinde yeni deploy almadı ve kayıtlı CLI token'ının süresi dolmuş olduğu için ben tetikleyemedim. İki seçenek:

- **Kolay:** Vercel Dashboard → siri-plan-app projesi → Deployments'a bakın. Deploy başarısız olduysa loguna, hiç yoksa Settings → Git bölümünden GitHub bağlantısına bakın (kopmuş olabilir).
- **Terminal:** `vercel login` → sonra proje klasöründe `vercel --prod`

### 2. Supabase SQL

Supabase Dashboard → **SQL Editor**'e şu dosyanın içeriğini yapıştırıp çalıştırın:


> `supabase/migrations/015_missing_columns_apply_me.sql`

Bu dosya: `staff.preferred_language` (personel dil tercihi — personel ekleme hatasının kaynağı) + `staff.color` (takvimde kalıcı personel rengi) kolonlarını ekler ve **yeni işletmeler için ücretsiz deneme süresini 7 güne** çeker (daha önce çalıştırdıysanız güncel halini tekrar çalıştırın — güvenlidir). **İdempotenttir** — yanlışlıkla iki kez çalıştırılsa bile sorun çıkmaz. 010/011/012/013 migration'larının canlıda zaten uygulanmış olduğu doğrulandı; eksik olan sadece buydu. Kod, bu SQL çalıştırılmadan da çökmeyecek şekilde yazıldı (kolon yoksa özellik sessizce devre dışı kalır).

---

## 🔑 Test Hesapları (canlı sistemde hazır)

**Salon:** Sirius Demo Güzellik Salonu (`sirius-demo-salon`, Pro plan)

| Rol | E-posta | Şifre | Kişi |
|---|---|---|---|
| İşletme Sahibi | `sahip.demo@siriplan.com` | `Sahip!2026Demo` | Selin Üstüay |
| Yönetici | `yonetici.demo@siriplan.com` | `Yonetici!2026Demo` | Murat Kaya |
| Personel (Saç) | `personel1.demo@siriplan.com` | `Personel1!2026` | Elif Demir |
| Personel (Cilt) | `personel2.demo@siriplan.com` | `Personel2!2026` | Can Yılmaz |

Platform admin paneli (`/admin`) sizin hesabınıza (`ustuayozgun@gmail.com`) tanımlı — test hesapları giremez (doğrulandı).

**Örnek müşteriler:** Ayşe Yıldız (9/10 sadakat puanı, boya alerjisi notu), Fatma Öztürk, Mehmet Aksoy — telefon, doğum günü, KVKK alanlarıyla.

**Örnek veriler:** 5 hizmet, 5 randevu (dün tamamlanan → otomatik gelir kaydı düştü ✓, bugün onaylı + onay bekleyen "acil" talep, yarın, gelecek hafta), kira/malzeme giderleri, ürün satış geliri, 2 sabit gider şablonu.

---

## 🐛 Bulunan ve Düzeltilen Hatalar

1. **Randevular hiçbir listede görünmüyordu (en kritik).** Migration 010 `appointments` tablosuna ikinci bir staff bağlantısı (`assigned_staff_id`) ekleyince Supabase'in `staff(...)` sorguları belirsizleşti ve **sessizce boş** dönmeye başladı. Randevu listesi, ana sayfa, raporlar, müşteri geçmişi boş görünüyor; randevu detay sayfası 404 atıyordu. 10 dosyada sorgular `staff!appointments_staff_id_fkey` ile netleştirildi. Hatırlatma cron'u ve Excel dışa aktarma da bundan etkileniyordu — düzeldi.
2. **Personel sayfası 500 veriyordu.** Next.js 16'da sunucu bileşenine `onClick` yazılamaz; tel/WhatsApp butonları client bileşene (`ContactLinks`) taşındı.
3. **Personel ekleme "preferred_language" hatası** — migration 015 ile çözülüyor (yukarıdaki tek SQL).
4. Müşteri detayına **WhatsApp butonu** eklendi, randevu geçmişi tarihlerine yıl eklendi.

## ✨ Yeni Özellikler

- **Takvimde kalıcı personel rengi:** Personel düzenleme sayfasına 10 renkli seçici eklendi; seçilen renk DB'ye kaydedilir ve takvimde o personelin tüm randevuları bu renkle görünür. Renk seçilmezse otomatik palet devam eder. (015 SQL'i çalıştırıldıktan sonra aktif.)
- Takvim zaten **gün / hafta / ay** görünümlerine, personel filtresine, 15 dk aralıklara ve canlı güncellemeye sahip — doğrulandı.

## ✅ Test Sonuçları

| Test | Sonuç |
|---|---|
| Production build | ✓ hatasız |
| 18 dashboard sayfası + 5 detay sayfası (login'li tarama) | ✓ tümü 200 |
| Resend e-postası | ✓ ustuayozgun@gmail.com'a gerçek test maili gönderildi (id: 20b96d3b) |
| 6 cron endpoint'i (reminder, noshow, birthday, weekly-champion, monthly-badges, score-refresh) | ✓ 200; yanlış secret → 401 |
| Güvenlik: personel kısıtlı 7 sayfaya giremedi, API 403, org izolasyonu (başka salonun randevusu 404), anon erişim 401, /admin koruması | ✓ 21/21 |
| Otomasyon: randevu "tamamlandı" → otomatik gelir kaydı + müşteri ziyaret/harcama/sadakat güncellemesi | ✓ doğrulandı |
| GitHub push (`7b26bd7`) | ✓ |
| Vercel deploy | ✗ otomatik tetiklenmedi — yukarıdaki 1. adım gerekli (CLI token süresi dolmuş) |

## 🔄 Ek Revizyon (aynı gün, 2. tur)

- **Ücretsiz deneme 14 → 7 gün:** Kayıt akışları (callback, quick-register), Stripe checkout (`trial_period_days`), DB varsayılanı (015 içinde) ve **62 metin** (TR/EN/RU/AR dil dosyaları, ana sayfa, fiyatlar, SSS, özellikler, kategori, kariyer, entegrasyonlar, demo, blog içerikleri, hoş geldin e-postası, chatbot yanıtları, llms.txt) güncellendi. Gizlilik/KVKK/koşullar sayfalarında süre ifadesi yoktu — kontrol edildi.
- **Plan seçim döngüsü düzeltildi:** Deneme süresi dolan kullanıcı girişte plan sayfasına yönlendirilince "ücretsiz denemeye devam et" tıklaması sessizce aynı sayfaya dönüyordu. Artık: süre dolduysa sarı uyarı banner'ı çıkıyor, "devam et" linki gizleniyor ve tıklanırsa açıklayıcı hata gösteriliyor; süre dolmadıysa buton doğrudan panele götürüyor (test edildi).
- **"Özgün" işletmenizin dolmuş deneme süresi +7 gün uzatıldı** — panele tekrar girebilirsiniz.

## 📌 Notlar

- WhatsApp Cloud API token'ları org bazında boş — WhatsApp otomatik mesajlar (hatırlatma vb.) salon ayarlarına Meta token'ı girilince aktifleşir. Müşteri listesindeki WhatsApp butonları bundan bağımsız, `wa.me` ile hemen çalışıyor.
- Test şifrelerini paylaşımdan sonra değiştirmek isterseniz Supabase Dashboard → Authentication → Users'tan sıfırlayabilirsiniz.
